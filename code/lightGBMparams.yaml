method: random
parameters:
  boosting_type:
    values: [gbdt]
  num_leaves:
    min: 2
    max: 100
  max_depth:
    min: 1
    max: 100
  learning_rate:
    min: 0.001
    max: 1.0
  feature_fraction:
    min: 0.001
    max: 1.0
  bagging_freq:
    min: 0
    max: 10
  # bagging_fraction:
  #   min: 0.1
  #   max: 1.0
  # lambda_l1:
  #   min: 0.0
  #   max: 10.0
  # lambda_l2:
  #   min: 0.0
  #   max: 10.0
  # min_data_in_leaf:
  #   min: 20
  #   max: 200
  # min_sum_hessian_in_leaf:
  #   min: 0.001
  #   max: 10.0
  # cat_smooth:
  #   min: 1
  #   max: 50
  metric:
    values: [binary_logloss]
